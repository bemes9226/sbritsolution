# Netlify redirects file for SPA routing
# This ensures all routes are handled by React Router instead of Netlify looking for files

# Redirect all routes to index.html with 200 status (rewrite, not redirect)
/*    /index.html   200
